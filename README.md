# Prototypes

HTML prototypes for new product ideas.

## Structure

- `prototypes/` - Individual prototype projects
- `shared/` - Shared CSS, JS, and assets

## Getting Started

Open any HTML file directly in a browser, or use a local server:

```bash
npx serve .
```
